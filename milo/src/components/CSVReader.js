import React from 'react';
import { CSVReader } from 'papaparse';
import { useAppContext } from '../AppProvider'; // Update the path accordingly

export default function CSVReaderComponent() {
    return (
        <input type="file"/>
    )
}
